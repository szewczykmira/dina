import { css } from 'lit';

export const styles = css`
  header {
    margin: 0 0 auto 0;
    padding: 2em;
    background-color: var(--grey);
    color: var(--teal);
    /*text*/
    text-align: center;
    font-style: italic;
    font-size: 1em;
    font-variant: small-caps;
  }
`;
