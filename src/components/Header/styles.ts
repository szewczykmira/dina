import { css } from 'lit';

export const styles = css`
  header {
    margin: 0 0 auto 0;
    background-color: var(--jet);
    color: var(--paradise-pink);
    padding: 2em;
    text-align: center;
  }
`;
