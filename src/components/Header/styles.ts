import { css } from 'lit';

export const styles = css`
  header {
    margin: 0 0 auto 0;
    padding: 2em;
    background-color: var(--jet);
    color: var(--paradise-pink);
    /*text*/
    text-align: justify;
    font-style: italic;
    font-size: 1em;
    font-variant: small-caps;
  }
`;
